// 과제 08
/* 문제1. 1부터 N까지 출력1
- 변수 `N` 을 선언하고, 임의의 숫자형 값을 할당한다.
- 변수 `N` 의 값을 출력한다.
- 1부터 `N` 까지 출력하는 `for` 반복문을 작성한다.
*/
let n = 5;
let count = 1;

console.log(n);
console.log();

for (let index = 1; index <= n; index++) {
  console.log(index);
}